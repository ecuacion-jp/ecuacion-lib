name: Back-merge main into develop

on:
  pull_request:
    types: [closed]
    branches: [main]

permissions:
  contents: write

jobs:
  back-merge:
    uses: ./.github/workflows/reusable-back-merge-develop.yml
